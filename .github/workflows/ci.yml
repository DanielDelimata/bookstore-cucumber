name: ci
on:
  push:
    branches: [ main ]
  pull_request:

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  BASE_URL: https://fakerestapi.azurewebsites.net
  TEST_ENVIRONMENT: github-actions
  LOG_LEVEL: INFO

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 21
          cache: gradle

      - name: Run tests
        env:
          BASE_URL: https://fakerestapi.azurewebsites.net
          LOG_LEVEL: INFO
        run: ./gradlew clean test --no-daemon --stacktrace

      - name: Upload test reports & results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-and-allure-results
          path: |
            build/reports/**
            build/test-results/**
            build/allure-results/**
          if-no-files-found: ignore

      - name: Setup Allure History
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages
        continue-on-error: true

      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@v1.11
        if: always()
        with:
          allure_results: build/allure-results
          gh_pages: gh-pages
          allure_history: allure-history
          allure_report: allure-report
          keep_reports: 20

      - name: Sanity check report dirs
        if: always()
        run: |
          ls -la build || true
          ls -la build/allure-results || true
          ls -la allure-history || true

      - name: Publish Allure Report
        if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.PERSONAL_TOKEN }}  # <-- poprawka! zamiast "token"
          publish_branch: gh-pages
          publish_dir: allure-history
          force_orphan: true